# HOLDER Price Monitoring Bot 📊

> Комплексная система мониторинга цены токена $HOLDER на DEX и CEX с Telegram ботом и Mini App

![HOLDER](https://img.shields.io/badge/Token-HOLDER-blue)
![Python](https://img.shields.io/badge/Python-3.11+-green)
![React](https://img.shields.io/badge/React-18+-blue)
![License](https://img.shields.io/badge/License-MIT-yellow)

## 📋 Содержание

- [Возможности](#-возможности)
- [Архитектура](#-архитектура)
- [Установка](#-установка)
- [Настройка](#-настройка)
- [Запуск](#-запуск)
- [Использование](#-использование)
- [API Документация](#-api-документация)
- [Docker Deployment](#-docker-deployment)
- [Разработка](#-разработка)
- [FAQ](#-faq)

## ✨ Возможности

### Telegram Bot
- 📊 **Реал-тайм мониторинг цен** на STON.fi DEX и WEEX CEX
- 📈 **Статистика за 24 часа**: high, low, volume, изменения
- 💹 **Арбитражные возможности**: автоматическое определение разницы цен
- 📉 **Графики цен**: 1h, 24h, 7d с использованием matplotlib
- 💼 **Портфолио трекинг**: отслеживание ваших холдингов и P/L
- 🔔 **Умные уведомления**: алерты о резких изменениях цены
- 🎯 **Настраиваемые пороги**: персональные настройки уведомлений
- 👥 **Групповые чаты**: работа в группах для коллективных уведомлений

### Telegram Mini App
- 🌐 **Веб-интерфейс** внутри Telegram
- 📊 **Интерактивные графики** с Chart.js
- ⚡ **WebSocket соединение** для реал-тайм обновлений
- 📱 **Адаптивный дизайн** для мобильных устройств
- 🎨 **Telegram тема** - автоматическое применение цветовой схемы
- 📈 **Сравнение DEX vs CEX** на одном графике

### Технические особенности
- 💾 **SQLite база данных** для хранения истории цен
- 🔄 **Автоматическая очистка** старых данных
- 📡 **API интеграции**:
  - STON.fi DEX (`EQCDuRLTylau8yKEkx1AMLpHAy6Vog_5D6aC4HNkyG8JN-me`)
  - Origami/WEEX CEX (symbol_id: 36380)
- 🐳 **Docker support** для легкого деплоя
- 🔒 **Безопасность**: переменные окружения для секретов

## 🏗 Архитектура

```
holder-price-bot/
├── bot/                      # Telegram Bot
│   ├── main.py              # Основной файл бота
│   ├── handlers/            # Обработчики команд
│   │   ├── commands.py      # Базовые команды
│   │   ├── portfolio.py     # Портфолио
│   │   ├── alerts.py        # Уведомления
│   │   └── charts.py        # Графики
│   └── utils/               # Утилиты
│
├── miniapp/                 # Telegram Mini App
│   ├── backend/            # FastAPI бэкенд
│   │   └── app/
│   │       └── main.py     # REST API + WebSocket
│   └── frontend/           # React фронтенд
│       ├── src/
│       │   ├── App.jsx     # Главный компонент
│       │   └── components/ # React компоненты
│       ├── package.json
│       └── vite.config.js
│
├── shared/                  # Общий код
│   ├── price_tracker.py    # Интеграция API
│   ├── database.py         # Работа с БД
│   └── charts.py           # Генерация графиков
│
├── config.py               # Конфигурация
├── requirements.txt        # Python зависимости
├── docker-compose.yml      # Docker setup
└── README.md
```

## 🚀 Установка

### Требования

- Python 3.11+
- Node.js 18+ (для Mini App frontend)
- Telegram Bot Token
- Docker (опционально)

### Шаг 1: Клонирование репозитория

```bash
git clone https://github.com/Memetrix/holder-price-bot.git
cd holder-price-bot
```

### Шаг 2: Установка Python зависимостей

```bash
# Создать виртуальное окружение (рекомендуется)
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Установить зависимости
pip install -r requirements.txt
```

### Шаг 3: Установка Frontend зависимостей (для Mini App)

```bash
cd miniapp/frontend
npm install
cd ../..
```

## ⚙️ Настройка

### 1. Получить Telegram Bot Token

1. Открой [@BotFather](https://t.me/BotFather) в Telegram
2. Отправь `/newbot`
3. Следуй инструкциям и получи токен
4. (Опционально) Настрой Mini App: `/newapp` → укажи URL вашего веб-сервера

### 2. Создать .env файл

```bash
cp .env.example .env
```

Отредактируй `.env`:

```env
BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11

CHECK_INTERVAL=60          # Проверка каждые 60 секунд
ALERT_THRESHOLD=5.0       # Уведомление при изменении >5%
ARB_THRESHOLD=2.0         # Арбитраж алерт при разнице >2%

DATABASE_PATH=data/holder_bot.db
MINIAPP_PORT=8000
LOG_LEVEL=INFO
```

## 🎮 Запуск

### Вариант 1: Локальный запуск

#### Запуск бота

```bash
python bot/main.py
```

#### Запуск Mini App Backend

```bash
# Терминал 2
python -m uvicorn miniapp.backend.app.main:app --host 0.0.0.0 --port 8000
```

#### Запуск Mini App Frontend (для разработки)

```bash
# Терминал 3
cd miniapp/frontend
npm run dev
```

Frontend будет доступен на `http://localhost:3000`

### Вариант 2: Docker Compose (Рекомендуется)

```bash
# Запустить всё сразу
docker-compose up -d

# Посмотреть логи
docker-compose logs -f

# Остановить
docker-compose down
```

Docker запустит:
- Telegram Bot (контейнер `holder-price-bot`)
- Mini App Backend (порт 8000)
- Mini App Frontend (порт 3000, dev server)

## 📱 Использование

### Команды Telegram бота

#### Основные команды

- `/start` - Главное меню с кнопками
- `/help` - Справка по всем командам
- `/price` - Текущие цены на DEX и CEX
- `/stats` - Статистика за 24 часа

#### Графики

- `/chart` - График за 24 часа
- `/chart 1h` - График за 1 час
- `/chart 7d` - График за 7 дней
- `/compare` - Сравнение DEX vs CEX

#### Арбитраж

- `/arbitrage` - Проверить текущие арбитражные возможности

#### Портфолио

- `/portfolio` - Посмотреть свой портфель
- `/portfolio add 1000 0.05` - Добавить 1000 HOLDER по цене $0.05
- `/portfolio remove 1` - Удалить запись #1

#### Уведомления

- `/alerts` - Меню настройки уведомлений
- `/alerts on` - Включить все уведомления
- `/alerts off` - Выключить уведомления
- `/alerts set 3` - Установить порог 3%

### Telegram Mini App

Откройте Mini App через кнопку в боте или специальную ссылку.

**Возможности:**
- 📊 Просмотр цен в реал-тайме
- 📈 Интерактивные графики (1h, 24h, 7d)
- 💹 Арбитражные возможности
- 📉 Детальная статистика

## 🔌 API Документация

### REST API Endpoints

Mini App Backend предоставляет REST API:

#### `GET /api/price`

Получить текущие цены

**Response:**
```json
{
  "success": true,
  "data": {
    "dex": {
      "source": "stonfi_dex",
      "pair": "HOLDER/TON",
      "price": 0.000123,
      "change_24h": 5.2,
      "volume_24h": 15000
    },
    "cex": {
      "source": "weex_cex",
      "pair": "HOLDER/USDT",
      "price": 0.000456,
      "change_24h": 3.8,
      "volume_24h": 25000
    }
  },
  "timestamp": "2024-11-15T10:30:00"
}
```

#### `GET /api/stats`

Получить статистику за 24ч

#### `GET /api/history?source=weex_cex&hours=24&limit=500`

Получить историю цен

**Parameters:**
- `source` - источник (weex_cex, stonfi_dex) [optional]
- `hours` - период в часах [default: 24]
- `limit` - макс. количество записей [default: 1000]

#### `GET /api/arbitrage`

Проверить арбитражные возможности

### WebSocket

Подключение: `ws://your-server:8000/ws`

**Messages:**
```json
{
  "type": "price_update",
  "data": {
    "dex": {...},
    "cex": {...}
  },
  "timestamp": "2024-11-15T10:30:00"
}
```

## 🐳 Docker Deployment

### Production Build

#### 1. Build образов

```bash
docker build -f Dockerfile.bot -t holder-price-bot:latest .
docker build -f Dockerfile.miniapp -t holder-miniapp:latest .
```

#### 2. Запуск с docker-compose

```bash
docker-compose up -d
```

#### 3. Мониторинг

```bash
# Логи бота
docker-compose logs -f bot

# Логи Mini App
docker-compose logs -f miniapp

# Статус
docker-compose ps
```

### Deploy на VPS

```bash
# На сервере
git clone https://github.com/Memetrix/holder-price-bot.git
cd holder-price-bot

# Настроить .env
nano .env

# Запустить
docker-compose up -d

# Настроить Nginx (для Mini App)
# Пример конфига Nginx:
```

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:8000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
    }
}
```

## 🛠 Разработка

### Добавление новой биржи

1. Отредактируй `shared/price_tracker.py`
2. Добавь новый метод `get_exchange_price()`
3. Добавь вызов в `get_all_prices()`
4. Обнови UI в боте и Mini App

### Добавление новой команды бота

1. Создай handler в `bot/handlers/`
2. Добавь в `bot/main.py`:
```python
from bot.handlers.new_feature import new_command

application.add_handler(CommandHandler("newcmd", new_command))
```

### Расширение Mini App

```bash
cd miniapp/frontend/src/components
# Создай новый компонент
touch NewComponent.jsx
```

### Тестирование

```bash
# Запустить с логами DEBUG
LOG_LEVEL=DEBUG python bot/main.py

# Проверить API
curl http://localhost:8000/api/price

# Проверить WebSocket
wscat -c ws://localhost:8000/ws
```

## ❓ FAQ

**Q: Бот не отвечает на команды**
A: Проверь правильность BOT_TOKEN в .env файле

**Q: Не находит HOLDER токен на STON.fi**
A: Убедись, что контрактный адрес правильный и токен действительно торгуется на STON.fi

**Q: Графики не генерируются**
A: Бот должен проработать некоторое время для накопления истории. Минимум 1 час данных нужно.

**Q: Mini App не подключается к WebSocket**
A: Проверь настройки CORS и порты в `miniapp/backend/app/main.py`

**Q: Как изменить интервал проверки цен?**
A: Измени `CHECK_INTERVAL` в .env файле (в секундах)

**Q: Можно ли использовать PostgreSQL вместо SQLite?**
A: Да, измени `shared/database.py` для использования PostgreSQL через SQLAlchemy

## 📊 Источники данных

### DEX - STON.fi
- **API**: https://api.ston.fi
- **Документация**: https://docs.ston.fi
- **Контракт HOLDER**: `EQCDuRLTylau8yKEkx1AMLpHAy6Vog_5D6aC4HNkyG8JN-me`

### CEX - WEEX (через Origami API)
- **API**: https://api.origami.tech
- **Symbol ID**: 36380

## 🤝 Contributing

Contributions are welcome! Please feel free to submit a Pull Request.

## 📄 License

MIT License - see LICENSE file for details

## 🙏 Acknowledgments

- Telegram Bot API
- STON.fi DEX
- WEEX Exchange
- FastAPI
- React + Vite
- Chart.js

## 📞 Support

Если возникли вопросы или проблемы:
- Создай [Issue](https://github.com/Memetrix/holder-price-bot/issues)
- Telegram: @your_support_contact

---

**Made with ❤️ for $HOLDER community** 🚀

⚠️ **Disclaimer**: Этот бот предоставляется "как есть" только в информационных целях. Не является финансовой рекомендацией. Всегда проводите собственное исследование перед принятием инвестиционных решений.
