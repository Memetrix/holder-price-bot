# HOLDER Price Monitoring Bot 📊

Telegram бот для мониторинга курса токена $HOLDER на DEX и CEX биржах с системой уведомлений о резких изменениях цены.

## Возможности

- 📊 Отслеживание цены HOLDER на DEX (Origami)
- 💱 Мониторинг пар HOLDER/USDT и HOLDER/TON на CEX
- 🔔 Автоматические уведомления о резких изменениях цены
- 📈 Статистика за 24 часа
- ⚡ Реал-тайм обновления
- 🎯 Настраиваемый порог уведомлений

## Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/yourusername/holder-price-bot.git
cd holder-price-bot
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

Или с использованием виртуального окружения:

```bash
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
pip install -r requirements.txt
```

### 3. Настройка

1. Создайте бота через [@BotFather](https://t.me/BotFather) и получите токен
2. Скопируйте `.env.example` в `.env`:

```bash
cp .env.example .env
```

3. Отредактируйте `.env` файл и добавьте ваш токен бота:

```env
BOT_TOKEN=your_bot_token_here
```

### 4. Настройка CEX API

По умолчанию бот подключен к DEX API (Origami). Для подключения CEX бирж:

1. Найдите публичные API endpoints для HOLDER/USDT и HOLDER/TON
2. Обновите методы `get_cex_usdt_price()` и `get_cex_ton_price()` в файле `price_tracker.py`
3. Укажите URL в `.env` файле (опционально)

## Запуск

```bash
python bot.py
```

Или с виртуальным окружением:

```bash
source venv/bin/activate  # На Windows: venv\Scripts\activate
python bot.py
```

## Использование

### Команды бота

- `/start` - Запуск бота и показ главного меню
- `/price` - Получить текущую цену HOLDER
- `/stats` - Показать статистику за 24 часа
- `/alerts` - Включить/выключить уведомления

### Кнопки

- 📊 **Current Price** - Текущий курс на всех биржах
- 📈 **24h Stats** - Статистика: максимум, минимум, объем
- 🔔 **Enable Alerts** - Включить уведомления о резких изменениях

## Настройка параметров

В файле `.env` вы можете настроить:

- `CHECK_INTERVAL` - Интервал проверки цены в секундах (по умолчанию 60)
- `ALERT_THRESHOLD` - Порог изменения цены для уведомлений в % (по умолчанию 5%)
- `LOG_LEVEL` - Уровень логирования (DEBUG, INFO, WARNING, ERROR)

## Структура проекта

```
holder-price-bot/
├── bot.py              # Основной файл бота
├── price_tracker.py    # Модуль отслеживания цен
├── config.py           # Конфигурация
├── requirements.txt    # Зависимости
├── .env.example        # Пример конфигурации
├── .gitignore          # Git ignore файл
└── README.md           # Документация
```

## API Источники

### DEX
- **Origami API**: `https://api.origami.tech/api/market/public/ticker?symbol_id=36380`

### CEX
- **HOLDER/USDT**: Требуется настройка (см. раздел "Настройка CEX API")
- **HOLDER/TON**: Требуется настройка (см. раздел "Настройка CEX API")

## Разработка

### Добавление новой биржи

1. Создайте новый метод в `PriceTracker` классе в `price_tracker.py`
2. Добавьте вызов метода в `get_all_prices()`
3. Обновите форматирование в `bot.py` для отображения данных

### Расширение функционала

Возможные улучшения:
- 📱 Telegram Mini App интерфейс
- 📊 Графики изменения цены
- 💾 База данных для хранения исторических данных
- 📧 Экспорт отчетов
- 🔄 Арбитражные возможности между биржами

## Требования

- Python 3.8+
- Telegram Bot Token

## Лицензия

MIT License

## Поддержка

Если у вас возникли вопросы или проблемы, создайте Issue в репозитории.

## Автор

Created for $HOLDER token community 🚀
